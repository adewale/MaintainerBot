# Required for higher GitHub API limits and draft PR creation.
GITHUB_TOKEN=github_pat_or_classic_token
GITHUB_OWNER=adewale

# Optional LLM review. Without this, MaintainerBot still produces deterministic scan reports.
FLUE_MODEL=anthropic/claude-haiku-4-5
ANTHROPIC_API_KEY=your-anthropic-key

# Safety gate. Keep false until you are ready for branch pushes/draft PRs.
CREATE_DRAFT_PRS=false

# Optional Cloudflare Email Routing delivery. Keep EMAIL_DRY_RUN=true until ready.
# You must enable Cloudflare Email Routing and verify EMAIL_TO first.
# EMAIL_FROM must be an address on the domain where Email Routing is active.
EMAIL_DRY_RUN=true
EMAIL_TO=you@example.com
EMAIL_FROM=maintainerbot@example.com

# Optional Cloudflare AI Gateway for Anthropic model traffic.
CLOUDFLARE_ACCOUNT_ID=your-account-id
CF_AI_GATEWAY_ID=your-gateway-id
CF_AI_GATEWAY_TOKEN=optional-gateway-token

# Optional gated draft PR creation.
DRAFT_PR_REPO_ALLOWLIST=adewale/example
