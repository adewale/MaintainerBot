node_modules
.env
.env.*
!.env.example
.webhook-secret
dist
.flue-vite
.flue-vite.wrangler.jsonc
.wrangler
.tmp
.DS_Store

# Logs can contain raw command output; do not commit them.
reports/*.log

# Keep dated daily report history in git, but ignore mutable "latest" files.
reports/daily-maintenance.md
reports/daily-maintenance.json
!reports/daily-maintenance-*.md
!reports/daily-maintenance-*.json
!reports/.gitkeep
